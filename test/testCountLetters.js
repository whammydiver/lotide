const assertEqual = require('../assertEqual.js');
const countLetters = require('../countLetters.js');

console.log(countLetters("hello hello hello hello"));
console.log(countLetters('what a worldly wanker wally white is!'));
console.log(countLetters('it was the best of times'));
